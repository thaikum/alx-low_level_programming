#include "main.h"

/**
 * print_sign - this function determines whether a number is negative, positive or 0
 * @n: the number to be checked;
 *
 * Return: + if positive, - if negative and 0 if 0
 */
int print_sign(int n)
{
	return (n > 0 ? '+' : n < 0 ? '-' : 0);
}
