#include "main.h"

/**
 * main - checks whether positive positive_or_negative is correct
 *
 * Return: 0 always
 */
int main(void)
{
	int result = positive_or_negative(0);

	if (result == 0)
		printf("0 is zero");
	return (0);
}
