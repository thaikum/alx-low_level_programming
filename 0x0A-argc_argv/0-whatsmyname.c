#include <stdio.h>

/**
 * main - prints the name of the program
 * @argc: size of argv
 * @argv: an array
 *
 * Return: 0 always
 */
int main(int argc, char *argv[])
{
	printf("%s", argv[0]);
	return (0);
}
